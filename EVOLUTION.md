# Evolution Log — business-intelligence

The loop that turns real presale outcomes into skill improvements. Mechanism: `../core/references/evolution-loop.md`. Harvest source: win/loss debriefs (`references/win-loss.md`), deal outcomes, discovery-call retros.

**The distinction that makes this matter:** `references/win-loss.md` learns about *deals* (why this one was won/lost). **This file learns about *the skill*** — after each debrief, ask "does this change BI's frameworks, positioning heuristics, or gates?" If a loss pattern recurs, it becomes a hypothesis here, not just a note in a deal file. Without this file, BI's per-deal learning evaporated and the skill stayed frozen at its authoring instinct.

## Harvest cadence
- **Per debrief:** one line — did anything here contradict or extend the skill? If yes, draft a pattern.
- **Quarterly (or per ~10 debriefs):** roll the patterns into an Evolution entry; apply the high Impact÷Effort ones; set verdicts.
- **Provenance honesty:** this skill was **authored from real presale practice, not formal training** — strong instinct, but its iterations were never *captured*. That's exactly what the evolution loop fixes. Treat long-standing heuristics that have never been win/loss-validated as `PENDING`, not gospel.

---

## Evolution 1 — 2026-07-01 — Establish the loop + enforce the honesty gate

### Harvest scope
- Not a deal harvest — a **structural audit** of the skill during a constellation-wide spine extraction (`core` kernel created; BI/SA/MS/skill-builder wired to it).
- Trigger: the observation that BI was "built on vibe, no formal training," and the counter-observation that it's actually well-formalized (Posture, win/loss.md, negative space, intelligence handoff) — the real gap was narrower than it looked.

### Patterns found
1. **No mechanism rolled deal-learning into skill-learning** — Impact: H, Effort: L. `win-loss.md` captures per-deal outcomes richly, but nothing fed recurring patterns back into the skill's frameworks/gates. BI learned about deals, never about itself. (This file is the fix.)
2. **The honesty posture wasn't an enforced gate** — Impact: H, Effort: L. Posture #2 ("demand evidence") was strong prose but not a declared gate with a recorded line, so grounding relied on discipline, not structure. (Now the Grounding Gate, inherited from `core`, with a required `Grounded: … → source, grade` line.)
3. **General discipline was implicit / duplicated across skills** — Impact: M, Effort: L. Integrity constraints, output contract, and the evolution loop lived (or didn't) per-skill. (Now inherited from `core`; BI references `../core/references/…`.)

### Hypotheses applied
1. **This `EVOLUTION.md`** — establishes the deal→skill learning loop; harvest source = win/loss debriefs. (Pattern 1.)
2. **Posture #2 elevated to the Grounding Gate** — `SKILL.md` §Posture-2 now requires a recorded `Grounded:` line + Admiralty grade, "a claim you cannot ground you do not make," pointing at `../core/references/grounding-gate.md`. (Pattern 2.)
3. **`core` inheritance** — `SKILL.md` Cross-links now names `core` as the kernel; BI references it rather than re-deriving integrity/output-contract/loop. (Pattern 3.)

Not modified (validated as already-strong): the 5-principle Posture, MEDDPICC qualification, executive/political profiling, negative-space discipline, win/loss interview bank, intelligence handoff.

### Validation results (fill after the next ~10 win/loss debriefs)
- [ ] At least one debrief pattern is promoted into an Evolution entry and changes a framework/battle-card/positioning heuristic.
- [ ] The `Grounded:` line + grade actually appears in intel outputs (gate is used, not just documented).
- [ ] A long-standing heuristic gets win/loss-*validated* (moves from instinct to evidence) — or *revised* by a loss pattern.
- **Verdict: PENDING** — structural changes from a single audit; needs real debrief cycles to confirm the loop compounds rather than sits idle.
